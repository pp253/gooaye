# 複製成 .env 並填入實際值（.env 已被 gitignore）

# OpenAI（逐字稿抽取用）
OPENAI_API_KEY=sk-...
# 抽取模型（GPT-5 mini 等級，省成本）
OPENAI_EXTRACT_MODEL=gpt-5-mini

# Supabase（local CLI 啟動後填入；npx supabase status 會印出）
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=
